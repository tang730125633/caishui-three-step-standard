# Step2：费用科目与角色绑定（合并版）

本步骤合并原“四步方案”中的 Step2 + Step3：

1. 添加二级费用科目（名称重复则复用已有ID）
2. 创建/复用费用角色组
3. 以“归属单据类型”创建/复用子角色
4. 绑定“单据适配人员 + 二级费用科目”

## 推荐执行顺序
1. `scripts/add_fee_templates.py`（先跑，产出映射）
2. `3a-添加费用角色-复用版.py`（只复用，不重复创建二级科目）

## 映射文件
- `fee_template_mapping.from_step2.json`

若名称重复，必须继续查询并复用ID，不视为失败。
